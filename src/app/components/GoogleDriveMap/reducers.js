export default function GoogleDriveMapReducer (state = {} , action) {
	let newState = {...state};

    switch (action.type) {
        case '':
        	return newState;
        default:
            return newState;
    }
}